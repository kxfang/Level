<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo">
        <item name="android:layout_margin">@dimen/ui_component_margin</item>
    </style>

</resources>
